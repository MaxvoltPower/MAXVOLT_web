{
  "name": "maxvolt",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm:dev:api\" \"npm:dev:frontend\"",
    "dev:api": "node server/index.mjs",
    "dev:frontend": "npm --prefix frontend run dev",
    "deploy": "vercel --prod",
    "seed": "node scripts/seed-products.mjs",
    "setup-indexes": "node scripts/setup-indexes.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "mongodb": "^6.3.0",
    "razorpay": "^2.9.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^17.4.2"
  },
  "engines": {
    "node": "24.x"
  }
}